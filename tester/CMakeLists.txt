OPTION(SYSU_TESTER "Build *.sysu.c as ref")
IF(SYSU_TESTER)
    FILE(GLOB _subs *)
    FOREACH(_sub ${_subs})
        IF(IS_DIRECTORY ${_sub})
            ADD_SUBDIRECTORY(${_sub})
        ENDIF()
    ENDFOREACH()
ENDIF()